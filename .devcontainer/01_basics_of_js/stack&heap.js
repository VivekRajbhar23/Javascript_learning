

//Stack(Premitive), Heap(Non-Premitive(reference))

//whenever we declare a premitive datatype ir will store in Stack memory and whenerver we will change the value it change in the copy of the
// value not original value.

//whenever we declare a non-premitive datatype ir will store in heap memory and whenerver we will change the value it change in the original of the
// value .